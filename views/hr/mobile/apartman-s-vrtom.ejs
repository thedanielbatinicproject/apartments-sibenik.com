<!DOCTYPE html>
<html lang="hr">
<head>
    <%- include('../../modules/head', { pageTitle: 'Apartman s vrtom', device: 'mobile' }) %>
</head>
<body>
    <%- include('../modules/header', { lang: 'hr', base: '/hr/mobile' }) %>
    <main>
        <section class="hero-section">
            <h1>Apartman s vrtom</h1>
            <p class="lead-text">Prostrani apartman s privatnim vrtom, idealan za obitelji.</p>
        </section>

        <section class="gallery-section">
            <h2>Galerija</h2>
            <%- include('../modules/gallery', { images }) %>
        </section>

        <section class="description-section">
            <h2>O apartmanu</h2>
            <p>Naš apartman s vrtom smješten je u mirnom dijelu Šibenika, nekoliko minuta hoda od plaže i centra. Savršen je za obitelji s djecom ili parove koji traže privatnost.</p>
            
            <div class="features">
                <div class="feature-item">
                    <h3>Sadržaji apartmana</h3>
                    <ul>
                        <li>Spavaća soba s bračnim krevetom</li>
                        <li>Dnevni boravak s kaučom</li>
                        <li>Potpuno opremljena kuhinja</li>
                        <li>Kupaonica s tušem</li>
                        <li>Privatni vrt</li>
                        <li>Klima uređaj i Wi-Fi</li>
                    </ul>
                </div>
            </div>
        </section>

        <section class="calendar-section">
            <h2>Rezervacije</h2>
            <p>Provjerite dostupnost apartmana:</p>
            <%- include('../modules/calendar', { calendarData: calendar1 }) %>
        </section>
    </main>
    <%- include('../modules/footer') %>
    <script src="/javascripts/header.js"></script>
    <script>
        // Dinamička promjena pozadine ovisno o dobu dana
        function setDynamicBackground() {
            const hour = new Date().getHours();
            let bg;
            if (hour >= 20 || hour < 6) {
                bg = 'var(--night-bg)';
            } else if (hour >= 6 && hour < 18) {
                bg = 'var(--day-bg)';
            } else {
                bg = 'var(--evening-bg)';
            }
            document.body.style.background = bg;
        }
        setDynamicBackground();
    </script>
</body>
</html>
