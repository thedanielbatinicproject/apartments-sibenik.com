<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/management/solar-dashboard.css">
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
    <div class="management-container">
        <div class="dashboard-header">
            <div class="header-main">
                <h1 class="dashboard-title">Glavna upravljačka ploča</h1>
                <p class="dashboard-subtitle">Apartments Šibenik - Administrativna ploča</p>
            </div>
            <div class="user-info" style="padding-top: 20px;">
                <span class="welcome-text">Dobrodošli, <%= user.username %>!</span>
                <% if (user.role === 'admin') { %>
                    <span class="admin-badge">Admin</span>
                <% } %>
                <form action="/management/logout" method="POST" style="display: inline;padding-left: 10px;">
                    <button type="submit" class="btn-danger">Odjavi se</button>
                </form>
            </div>
        </div>

        <div class="dashboard-grid">
            <div class="dashboard-card">
                <h3 class="card-title">🌞 Solarna elektrana</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Nadzirite uživo podatke sa solara u dvorištu.
                </p>
                <a href="/management/solar-dashboard" style="
                    display: inline-block;
                    padding: 0.8rem 1.5rem;
                    background: rgba(74, 222, 128, 0.2);
                    color: #4ade80;
                    text-decoration: none;
                    border-radius: 12px;
                    border: 1px solid rgba(74, 222, 128, 0.3);
                    font-weight: 600;
                    transition: all 0.3s ease;
                " onmouseover="this.style.background='rgba(74, 222, 128, 0.3)'" 
                   onmouseout="this.style.background='rgba(74, 222, 128, 0.2)'">
                    Otvori Dashboard
                </a>
            </div>

            <div class="dashboard-card">
                <h3 class="card-title">⭐ Recenzije</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Dodajte, uredite ili izbrišite recenzije na web stranici.
                </p>
                <a href="/management/reviews" style="
                    display: inline-block;
                    padding: 0.8rem 1.5rem;
                    background: rgba(251, 191, 36, 0.2);
                    color: #fbbf24;
                    text-decoration: none;
                    border-radius: 12px;
                    border: 1px solid rgba(251, 191, 36, 0.3);
                    font-weight: 600;
                    transition: all 0.3s ease;
                " onmouseover="this.style.background='rgba(251, 191, 36, 0.3)'" 
                   onmouseout="this.style.background='rgba(251, 191, 36, 0.2)'">
                    Uredi recenzije
                </a>
            </div>

            <div class="dashboard-card">
                <h3 class="card-title">📊 Računi</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Generirajte, izbrišite ili pregledajte račune.
                </p>
                <a href="/management/invoices" style="
                    display: inline-block;
                    padding: 0.8rem 1.5rem;
                    background: rgba(104, 251, 36, 0.2);
                    color: #87ff4f;
                    text-decoration: none;
                    border-radius: 12px;
                    border: 1px solid rgba(104, 251, 36, 0.3);
                    font-weight: 600;
                    transition: all 0.3s ease;
                    box-shadow: #87ff4f 0 4px 8px;
                " onmouseover="this.style.background='rgba(104, 251, 36, 0.3)'" 
                   onmouseout="this.style.background='rgba(104, 251, 36, 0.2)'">
                    Portal sa računima
                </a>
            </div>

            <div class="dashboard-card">
                <h3 class="card-title">🏠 Upravljanje sa jedinicama</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Upravljajte sa rezervacijama, kalendarima i samim jedinicama.
                </p>
                <div class="card-status status-offline">Dolazi uskoro</div>
            </div>

            <% if (user.role === 'admin') { %>
            <div class="dashboard-card">
                <h3 class="card-title">👥 Uređivanje računa</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Dodajte, uklonite i upravljajte korisnicima sistema i njihovim dozvolama.
                </p>
                <button onclick="openUserManagement()" style="
                    display: inline-block;
                    padding: 0.8rem 1.5rem;
                    background: rgba(139, 92, 246, 0.2);
                    color: #c8b9ea;
                    border: 1px solid rgba(139, 92, 246, 0.3);
                    border-radius: 12px;
                    font-weight: 600;
                    cursor: pointer;
                    transition: all 0.3s ease;
                " onmouseover="this.style.background='rgba(139, 92, 246, 0.3)'" 
                   onmouseout="this.style.background='rgba(139, 92, 246, 0.2)'">
                    Uredi korisnike
                </button>
            </div>
            
            <div class="dashboard-card">
                <h3 class="card-title">⚙️ Postavke</h3>
                <p style="color: rgba(255,255,255,0.8); margin-bottom: 1rem;">
                    Konfigurirajte postavke sustava i korisničke preferencije.
                </p>
                <div class="card-status status-offline">Dolazi uskoro</div>
            </div>
            <% } %>
        </div>

        <!-- Relay Control Section -->
        <div class="relay-controls-section">
            <h2 class="relay-controls-title">⚡ Upravljanje prekidačima</h2>
            <div class="relay-grid">
                <% for (let i = 1; i <= 4; i++) { %>
                    <% 
                    const relayKey = `relay${i}`;
                    const relayState = relayStates[relayKey] || false;
                    const cardClass = relayState ? 'powered-on' : 'powered-off';
                    const statusText = relayState ? 'Powered ON' : 'Powered OFF';
                    const actionText = relayState ? 'Press to TURN OFF' : 'Press to TURN ON';
                    %>
                    <div class="relay-toggle-card <%= cardClass %>" 
                         data-relay="<%= i %>" 
                         onclick="toggleRelay(<%= i %>)">
                        <div class="relay-title">RELAY <%= i %></div>
                        <img src="/images/icons/power_on.png" alt="Power Icon" class="relay-icon">
                        <div class="relay-status-text"><%= statusText %></div>
                        <div class="relay-action-text"><%= actionText %></div>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- User Management Modal (Admin Only) -->
        <% if (user.role === 'admin') { %>
        <div id="userManagementModal" class="modal" style="display: none;">
            <div class="modal-content">
                <div class="modal-header">
                    <h2>Upravljanje računima</h2>
                    <span class="close" onclick="closeUserManagement()">&times;</span>
                </div>
                <div class="modal-body">
                    <div class="user-actions">
                        <button onclick="showAddUserForm()" class="btn-primary">Dodaj novog korisnika</button>
                        <button onclick="loadUsers()" class="btn-secondary">Osvježi popis</button>
                    </div>
                    
                    <div id="addUserForm" style="display: none;">
                        <h3>Dodaj novog korisnika</h3>
                        <form id="newUserForm">
                            <input type="text" id="newUserName" placeholder="Full Name" required>
                            <input type="text" id="newUserUsername" placeholder="Username" required>
                            <input type="password" id="newUserPassword" placeholder="Password" required>
                            <button type="submit" class="btn-primary">Kreiraj korisnika</button>
                            <button type="button" onclick="hideAddUserForm()" class="btn-secondary">Odustani</button>
                        </form>
                    </div>
                    
                    <div id="usersList"></div>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Custom Alert Modal -->
        <div id="customAlert" class="modal" style="display: none;">
            <div class="modal-content" style="max-width: 400px; text-align: center;">
                <div class="modal-body" style="padding: 2rem;">
                    <div id="alertMessage" style="color: #fff; font-size: 1.1rem; margin-bottom: 1.5rem; line-height: 1.4;"></div>
                    <button onclick="closeCustomAlert()" class="btn-primary" style="min-width: 100px;">OK</button>
                </div>
            </div>
        </div>

        <!-- Custom Confirm Modal -->
        <div id="customConfirm" class="modal" style="display: none;">
            <div class="modal-content" style="max-width: 450px; text-align: center;">
                <div class="modal-body" style="padding: 2rem;">
                    <div id="confirmMessage" style="color: #fff; font-size: 1.1rem; margin-bottom: 1.5rem; line-height: 1.4;"></div>
                    <div style="display: flex; justify-content: center; gap: 1rem;">
                        <button onclick="confirmAction()" class="btn-danger" style="min-width: 100px;">Yes</button>
                        <button onclick="closeCustomConfirm()" class="btn-secondary" style="min-width: 100px;">Cancel</button>
                    </div>
                </div>
            </div>
        </div>

        <script>
            // Make current user available to JavaScript
            window.currentUser = {
                uuid: '<%= user.uuid %>',
                username: '<%= user.username %>',
                role: '<%= user.role %>'
            };
        </script>
        <script src="/javascripts/userManagement.js"></script>
        
        <style>
            /* Additional styles for change password modal */
            .user-item {
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 1rem;
                margin: 0.5rem 0;
                background: rgba(255, 255, 255, 0.1);
                border-radius: 8px;
                border: 1px solid rgba(255, 255, 255, 0.2);
            }
            
            .user-actions {
                display: flex;
                gap: 0.5rem;
                align-items: center;
            }
            
            .btn-warning {
                background: linear-gradient(135deg, rgba(251, 191, 36, 0.2) 0%, rgba(245, 158, 11, 0.2) 100%);
                color: #fbbf24;
                border: 1px solid rgba(251, 191, 36, 0.3);
                padding: 0.5rem 1rem;
                border-radius: 8px;
                cursor: pointer;
                font-weight: 600;
                font-size: 0.9rem;
                transition: all 0.3s ease;
                backdrop-filter: blur(10px);
            }
            
            .btn-warning:hover {
                background: linear-gradient(135deg, rgba(251, 191, 36, 0.3) 0%, rgba(245, 158, 11, 0.3) 100%);
                transform: translateY(-1px);
                box-shadow: 0 4px 12px rgba(251, 191, 36, 0.25);
            }
            
            #changePasswordModal .modal-content {
                max-width: 500px;
            }
            
            .form-group {
                margin-bottom: 1rem;
            }
            
            .form-group label {
                display: block;
                margin-bottom: 0.5rem;
                color: #fff;
                font-weight: 600;
            }
            
            .form-group input {
                width: 100%;
                padding: 0.75rem;
                border: 1px solid rgba(255, 255, 255, 0.3);
                border-radius: 8px;
                background: rgba(255, 255, 255, 0.1);
                color: #fff;
                font-size: 1rem;
                backdrop-filter: blur(10px);
                box-sizing: border-box;
            }
            
            .form-group input:focus {
                outline: none;
                border-color: rgba(74, 222, 128, 0.5);
                box-shadow: 0 0 0 2px rgba(74, 222, 128, 0.2);
            }
            
            .form-group input::placeholder {
                color: rgba(255, 255, 255, 0.5);
            }
            
            .form-actions {
                display: flex;
                gap: 1rem;
                justify-content: flex-end;
                margin-top: 1.5rem;
            }
        </style>
    </div>
</body>
</html>
