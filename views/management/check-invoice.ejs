<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #2c5aa0 0%, #1e3a5f 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
            padding-top: 80px; /* Space for header */
        }

        .container {
            background: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
            max-width: 500px;
            width: 100%;
            margin: 20px;
        }

        .logo {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            background: #2c5aa0;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            font-weight: bold;
        }

        h1 {
            color: #2c5aa0;
            margin-bottom: 10px;
            font-size: 28px;
        }

        .subtitle {
            color: #666;
            margin-bottom: 30px;
            font-size: 16px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #2c5aa0;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e1e1e1;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s ease;
            text-align: center;
            font-family: 'Courier New', monospace;
            font-weight: bold;
            letter-spacing: 1px;
        }

        input[type="text"]:focus {
            outline: none;
            border-color: #2c5aa0;
            box-shadow: 0 0 0 3px rgba(44, 90, 160, 0.1);
        }

        .btn {
            background: linear-gradient(135deg, #2c5aa0 0%, #1e3a5f 100%);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            width: 100%;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(44, 90, 160, 0.3);
        }

        .btn:active {
            transform: translateY(0);
        }

        .error {
            background: #fee;
            border: 1px solid #fcc;
            color: #c33;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .help-text {
            margin-top: 20px;
            font-size: 12px;
            color: #888;
            line-height: 1.5;
        }

        .back-link {
            margin-top: 20px;
        }

        .back-link a {
            color: #2c5aa0;
            text-decoration: none;
            font-size: 14px;
        }

        .back-link a:hover {
            text-decoration: underline;
        }

        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
                margin: 10px;
            }

            h1 {
                font-size: 24px;
            }

            .subtitle {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <%- include('../modules/header', { lang: 'en', base: '/en', isAuthenticated: isAuthenticated }) %>
    
    <div class="container">
        <div class="logo">
            <img src="/images/icons/invoice.png" alt="Invoice" style="width: 40px; height: 40px;">
        </div>
        
        <h1>Check Invoice</h1>
        <p class="subtitle">Verify your invoice by entering the UID below</p>

        <% if (error) { %>
            <div class="error">
                <%= error %>
            </div>
        <% } %>

        <form method="POST" action="/check-invoice">
            <div class="form-group">
                <label for="uid">Invoice UID:</label>
                <input 
                    type="text" 
                    id="uid" 
                    name="uid" 
                    placeholder="Enter invoice UID (e.g. A53F-B61A)"
                    required
                    maxlength="50"
                >
            </div>

            <button type="submit" class="btn">
                Check Invoice
            </button>
        </form>

        <div class="help-text">
            The Invoice UID can be found at the bottom of your original invoice document. 
            It's a unique identifier that helps us verify your invoice.
        </div>

        <div class="back-link">
            <a href="/">← Back to Homepage</a>
        </div>
    </div>

    <script>
        // Auto-focus on the input field
        document.getElementById('uid').focus();
        
        // Auto-uppercase input
        document.getElementById('uid').addEventListener('input', function(e) {
            e.target.value = e.target.value.toUpperCase();
        });
    </script>
</body>
</html>
