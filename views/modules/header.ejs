<link rel="stylesheet" href="/stylesheets/index/header.css">

<header class="main-header">
  <div class="header-container">
    <!-- Logo and Title -->
    <div class="header-logo">
      <a href="/" class="logo-link">
        <img src="/images/logo.png" alt="Logo" class="logo-image">
      </a>
      <a href="/" class="logo-text-link">
        <span class="logo-text">Apartments Šibenik</span>
      </a>
    </div>

    <!-- Desktop Navigation -->
    <nav class="desktop-nav">
      <ul class="nav-menu">
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/apartman-s-vrtom' %>" class="nav-link">Apartman s vrtom</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/studio-apartman' %>" class="nav-link">Studio apartman</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/soba' %>" class="nav-link">Soba</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/o-sibeniku' %>" class="nav-link">O Šibeniku</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/kontakt' %>" class="nav-link contact-btn">Kontakt</a></li>
      </ul>
    </nav>

    <!-- Mobile Hamburger Menu -->
    <div class="mobile-menu-toggle" id="mobileMenuToggle">
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </div>
  </div>

  <!-- Mobile Menu Overlay -->
  <div class="mobile-menu-overlay" id="mobileMenuOverlay">
    <div class="mobile-menu-content">
      <ul class="mobile-nav-menu">
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/apartman-s-vrtom' %>" class="mobile-nav-link">Apartman s vrtom</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/studio-apartman' %>" class="mobile-nav-link">Studio apartman</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/soba' %>" class="mobile-nav-link">Soba</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/o-sibeniku' %>" class="mobile-nav-link">O Šibeniku</a></li>
        <li><a href="<%= (typeof base !== 'undefined' ? base : '') + '/kontakt' %>" class="mobile-nav-link mobile-contact-btn">Kontakt</a></li>
      </ul>
      <div class="mobile-menu-close" id="mobileMenuClose">
        <span>&times;</span>
      </div>
    </div>
  </div>
</header>

<script src="/javascripts/header.js"></script>
